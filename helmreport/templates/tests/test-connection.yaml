apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmreport.fullname" . }}-test-connection"
  labels:
    {{- include "helmreport.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helmreport.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "helmreport.fullname" . }}
  labels:
    {{- include "helmreport.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helmreport.selectorLabels" . | nindent 4 }}
